# 보안 및 데이터 제출 점검

업로드된 프로젝트에는 개발용 자격증명 파일과 키가 포함된 흔적이 있습니다. 제출·배포 전에 다음을 수행합니다.

1. 노출된 API 키와 서비스 계정 키는 즉시 폐기하고 재발급합니다.
2. `.env`, `serviceAccountKey.json`, 로그·스크린샷 속 키/이메일을 저장소와 시연자료에서 제거합니다.
3. 서버 비밀값은 환경변수로만 주입하고, 프론트엔드에는 공개 가능한 API 주소만 둡니다.
4. 사진·영수증은 기본적으로 기기에 오래 보관하지 않고, 서버 전송 시 동의·삭제 정책을 안내합니다.
